# Pier job (optimistic, DGX Spark, KIMI): full-spark-optimistic.yaml against
# subconscious/tim-kimi2.6 (pass rates below were measured with QWEN — they
# are a task shortlist, not a kimi prediction). ONE attempt each.
#   ./mini-swe-agent/run_spark_optimistic_kimi.sh
# Qwen: full-spark-optimistic.yaml • Kimi full eval: full-spark-kimi.yaml

n_concurrent_trials: 8
n_attempts: 1

agents:
  - name: mini-swe-agent
    model_name: subconscious/tim-kimi2.6
    env:
      OPENAI_API_KEY: ${BASETEN_API_KEY}
      OPENAI_BASE_URL: ${BASETEN_BASE_URL}
      MSWEA_API_KEY: ${BASETEN_API_KEY}          # required for custom model slugs
      MSWEA_MODEL_RETRY_STOP_AFTER_ATTEMPT: "5"  # 5xx/connection retries; timeouts never retry
      # custom pricing, read at trial start (re-price later: scripts/reprice.py)
      LITELLM_MODEL_REGISTRY_PATH: /opt/deep-swe/pricing/model-registry.json
      MSWEA_COST_TRACKING: ignore_errors # don't kill turns on cost-calc errors
      PYTHONPATH: /opt/deep-swe/scripts          # auto-loads scripts/sitecustomize.py timing probe
      MSWEA_TIMING_LOG: /logs/agent/llm_timing.jsonl
    kwargs:
      # timeouts / max-token truncations become single failed turns instead of
      # retry storms — see scripts/turn_failure_model.py
      model_class: turn_failure_model.TurnFailureModel
      version: "2.3.0"  # pinned (turn_failure_model subclasses internals); re-run prebuild.sh after changing
      cost_limit: 0
      model_kwargs:
        custom_llm_provider: openai  # chat completions against OPENAI_BASE_URL
        num_retries: 0               # litellm-internal retries off; MSWEA_* above handles them
        timeout: 480                  # per-request cap; a timeout costs one failed turn
        max_tokens: 16384            # per-completion output cap; truncation costs one failed turn
        temperature: 0.7
        top_p: 0.95

datasets:
  - path: tasks
    # >=50% average pass rate on prior benchmark runs, descending:
    task_names:
      - true-myth-iterable-collection-combinators  # 76%
      - happy-dom-abort-pending-body-reads  # 75%
      - returns-validated-error-accumulation  # 65%
      - ofetch-per-origin-circuit-breaker  # 64%
      - drizzle-orm-window-function-builders  # 63%
      - sql-formatter-bigquery-pipe-formatting  # 62%
      - ytt-jsonpath-query-api  # 61%
      - httpx-deterministic-cookie-store  # 60%
      - psd-tools-blend-range-api  # 59%
      - actionlint-action-pinning-lint  # 58%
      - wazero-multi-module-snapshots  # 58%
      - query-persist-restored-query-state  # 57%
      - yjs-map-conflict-detection  # 57%
      - vulture-persistent-analysis-cache  # 56%
      - testem-per-launcher-reports  # 55%
      - abs-module-cache-flags  # 53%
      - ts-pattern-match-each  # 51%
      - abs-stepped-slices  # 51%
      - arcane-drift-detection-baselines  # 50%
      - koota-entity-snapshot-rollback  # 50%
      - obsidian-linter-scoped-ignore-markers  # 50%
      - aiomonitor-task-snapshots-diff  # 50%
      # avg % missing from the source list (likely <50%) — uncomment to include:
      # - helm-unified-manifest-stream

environment:
  type: docker
  # aarch64: prebuilt task images are amd64-only — build natively
  # (warm the cache first: ./scripts/prebuild.sh)
  force_build: true
  # `mounts` REPLACES Pier's default /logs + /artifacts binds — keep the first three.
  mounts:
    - type: bind
      source: ${HOST_VERIFIER_LOGS_PATH}
      target: ${ENV_VERIFIER_LOGS_PATH}
    - type: bind
      source: ${HOST_AGENT_LOGS_PATH}
      target: ${ENV_AGENT_LOGS_PATH}
    - type: bind
      source: ${HOST_ARTIFACTS_PATH}
      target: ${ENV_ARTIFACTS_PATH}
    # pricing registry (LITELLM_MODEL_REGISTRY_PATH above)
    - type: bind
      source: ${DEEP_SWE_ROOT}/pricing
      target: /opt/deep-swe/pricing
      read_only: true
    # sitecustomize.py + turn_failure_model.py (PYTHONPATH above)
    - type: bind
      source: ${DEEP_SWE_ROOT}/scripts
      target: /opt/deep-swe/scripts
      read_only: true

jobs_dir: jobs
