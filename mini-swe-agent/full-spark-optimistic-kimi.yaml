# Pier job (optimistic full eval, DGX Spark, KIMI): subset run of the 22 tasks with
# a >=50% average pass rate on previous benchmarks, ONE attempt each (instead
# of 4) — a fast high-signal run, NOT leaderboard-comparable (subset + single
# trial). 22 trials total at 8 concurrent.
#
# Kimi variant — model subconscious/tim-kimi2.6 ($0.50/1M in miss,
# $0.15/1M in hit, $3.00/1M out). NOTE: the
# >=50% pass rates below were measured with QWEN; they are a task shortlist,
# not a kimi prediction.
# Derived from full-spark.yaml — agent/env/mounts/model settings
# are identical and should be kept in sync; only the header, n_attempts, and
# datasets.task_names differ.
#
#   ./scripts/prebuild.sh          # task images + agent overlays (all or subset)
#   ./mini-swe-agent/run_spark_optimistic_kimi.sh   # wraps: pier-run.sh run -c <this yaml> -y
#
# Resume after interrupt (same YAML; do not change config between start and resume):
#   pier job resume -p jobs/<job-dir>
#
# Qwen variant: full-spark-optimistic.yaml • Kimi full eval: full-spark-kimi.yaml

n_concurrent_trials: 8
n_attempts: 1

agents:
  - name: mini-swe-agent
    # Keep in sync with BASETEN_MODEL in .env
    model_name: subconscious/tim-kimi2.6
    env:
      OPENAI_API_KEY: ${BASETEN_API_KEY}
      OPENAI_BASE_URL: ${BASETEN_BASE_URL}
      # Required for custom model slugs Pier/LiteLLM don't recognize (e.g. subconscious/...)
      MSWEA_API_KEY: ${BASETEN_API_KEY}
      # Retry transient API errors (5xx/connection) up to 4 times. Timeouts
      # never retry — turn_failure_model.py converts them to failed turns.
      MSWEA_MODEL_RETRY_STOP_AFTER_ATTEMPT: "5"
      # mini-swe-agent 2.3.0 reads this (LitellmModelConfig.litellm_model_registry)
      # and calls litellm.utils.register_model() — custom pricing wired natively,
      # so per-call cost lands in the trajectory and result.json. pricing/ is
      # bind-mounted (dir) under environment.mounts below; point this at any
      # file in it to switch pricing curves — no rebuild. Re-price past runs
      # with scripts/reprice.py.
      LITELLM_MODEL_REGISTRY_PATH: /opt/deep-swe/pricing/model-registry.json
      # Safety net: if pricing registration ever fails, swallow the cost-calc
      # error (cost reads 0) instead of killing every turn. Verify cost is
      # nonzero on a dev run after touching pricing/.
      MSWEA_COST_TRACKING: ignore_errors
      # sitecustomize.py timing probe (auto-imported via PYTHONPATH): one JSON
      # line per LLM call (latency, tokens, tok/s, finish_reason, errors) →
      # analyze with scripts/tps_from_logs.py jobs/<job-dir>
      PYTHONPATH: /opt/deep-swe/scripts
      MSWEA_TIMING_LOG: /logs/agent/llm_timing.jsonl
    kwargs:
      # Custom LitellmModel subclass (scripts/turn_failure_model.py, importable
      # via the PYTHONPATH mount): request timeouts are NOT retried and
      # max-token truncations get a targeted reprompt — both become failed
      # turns the agent recovers from, instead of 10x retry storms or dead
      # episodes. Subclasses LitellmModel, so it keeps the LiteLLM chat
      # completions path (avoid the openai/ prefix — that routes to Responses API).
      model_class: turn_failure_model.TurnFailureModel
      # Pin mini-swe-agent: turn_failure_model.py subclasses its internals
      # (query/_parse_actions/abort_exceptions). Note: changing the pin changes
      # the generated agent-install Dockerfile layer — re-run
      # ./scripts/prebuild.sh to warm the overlay layers before the eval.
      version: "2.3.0"
      cost_limit: 0
      model_kwargs:
        # Base URL comes from OPENAI_BASE_URL in agent.env (LiteLLM reads it from the sandbox env)
        custom_llm_provider: openai
        # Keep LiteLLM-internal retries off; MSWEA_MODEL_RETRY_STOP_AFTER_ATTEMPT
        # above handles retries (the two would multiply otherwise)
        num_retries: 0
        # Per-request timeout (litellm-level, not sent to the server). Timeouts
        # are NOT retried (turn_failure_model.py converts them to one failed
        # turn), so this is the max a worker spends on one bad request. Sized
        # for 8 concurrent trials sharing the endpoint: a near-cap 16k response
        # at a contended ~40-60 tok/s/stream takes ~4.5-7 min, so 480s lets
        # slow-but-legit responses finish while still bounding runaways.
        timeout: 480
        # Per-completion output cap: 16k fits even dense write turns; if a step
        # truncates mid-edit the agent recovers and continues across steps
        # (turn_failure_model discards the truncated text + reprompts)
        max_tokens: 16384
        # Sampling params (passed through to the completion request)
        temperature: 0.7
        top_p: 0.95

datasets:
  - path: tasks
    # >=50% average pass rate on prior benchmark runs, descending:
    task_names:
      - true-myth-iterable-collection-combinators  # 76%
      - happy-dom-abort-pending-body-reads  # 75%
      - returns-validated-error-accumulation  # 65%
      - ofetch-per-origin-circuit-breaker  # 64%
      - drizzle-orm-window-function-builders  # 63%
      - sql-formatter-bigquery-pipe-formatting  # 62%
      - ytt-jsonpath-query-api  # 61%
      - httpx-deterministic-cookie-store  # 60%
      - psd-tools-blend-range-api  # 59%
      - actionlint-action-pinning-lint  # 58%
      - wazero-multi-module-snapshots  # 58%
      - query-persist-restored-query-state  # 57%
      - yjs-map-conflict-detection  # 57%
      - vulture-persistent-analysis-cache  # 56%
      - testem-per-launcher-reports  # 55%
      - abs-module-cache-flags  # 53%
      - ts-pattern-match-each  # 51%
      - abs-stepped-slices  # 51%
      - arcane-drift-detection-baselines  # 50%
      - koota-entity-snapshot-rollback  # 50%
      - obsidian-linter-scoped-ignore-markers  # 50%
      - aiomonitor-task-snapshots-diff  # 50%
      - helm-unified-manifest-stream  # 50%

environment:
  type: docker
  # The DGX Spark is aarch64 and the prebuilt task images (task.toml
  # [environment].docker_image) are amd64-only — force native arm64 builds from
  # each task's environment/Dockerfile (mars-base is multi-arch). Warm the
  # layer cache before the eval with ./scripts/prebuild.sh so the per-trial
  # `docker compose build` is a near-instant cache hit.
  force_build: true
  # NOTE: setting `mounts` REPLACES Pier's default /logs + /artifacts bind mounts
  # (pier/environments/docker/docker.py: mounts_json overrides _default_log_mounts).
  # Without them, tee /logs/agent/... exits 1 (NonZeroAgentExitCodeError) and no
  # trajectory/reward files are written — so re-add them here. The HOST_*/ENV_*
  # variables are injected per-trial by Pier when it invokes docker compose.
  mounts:
    - type: bind
      source: ${HOST_VERIFIER_LOGS_PATH}
      target: ${ENV_VERIFIER_LOGS_PATH}
    - type: bind
      source: ${HOST_AGENT_LOGS_PATH}
      target: ${ENV_AGENT_LOGS_PATH}
    - type: bind
      source: ${HOST_ARTIFACTS_PATH}
      target: ${ENV_ARTIFACTS_PATH}
    # Pricing registry dir for LITELLM_MODEL_REGISTRY_PATH (agents[].env
    # above). Directory mount: add models / edit curves in pricing/ without
    # touching this yaml — read at trial start, never baked into images.
    - type: bind
      source: ${DEEP_SWE_ROOT}/pricing
      target: /opt/deep-swe/pricing
      read_only: true
    # sitecustomize.py (timing probe) + turn_failure_model.py, importable via
    # the PYTHONPATH env var in agents[].env above.
    - type: bind
      source: ${DEEP_SWE_ROOT}/scripts
      target: /opt/deep-swe/scripts
      read_only: true

jobs_dir: jobs
