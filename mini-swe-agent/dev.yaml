# Pier job (dev): quick smoke / iteration — NOT leaderboard-comparable.
# Caps: 30 min agent timeout, 600 steps, 1 worker, task(s) pinned below.
#   ./mini-swe-agent/run_dev.sh [-p tasks/<task-id>]
# Resume: pier job resume -p jobs/<job-dir>
# Full eval: full.yaml • Kimi: dev-kimi.yaml

n_concurrent_trials: 1

agents:
  - name: mini-swe-agent
    override_timeout_sec: 1800
    model_name: subconscious/tim-qwen3.6-27b
    env:
      OPENAI_API_KEY: ${BASETEN_API_KEY}
      OPENAI_BASE_URL: ${BASETEN_BASE_URL}
      MSWEA_API_KEY: ${BASETEN_API_KEY}          # required for custom model slugs
      MSWEA_MODEL_RETRY_STOP_AFTER_ATTEMPT: "5"  # 5xx/connection retries; timeouts never retry
      # custom pricing, read at trial start (re-price later: scripts/reprice.py)
      LITELLM_MODEL_REGISTRY_PATH: /opt/deep-swe/pricing/model-registry.json
      MSWEA_COST_TRACKING: default       # dev canary: fail loudly if pricing unregistered
      PYTHONPATH: /opt/deep-swe/scripts          # auto-loads scripts/sitecustomize.py timing probe
      MSWEA_TIMING_LOG: /logs/agent/llm_timing.jsonl
    kwargs:
      # timeouts / max-token truncations become single failed turns instead of
      # retry storms — see scripts/turn_failure_model.py
      model_class: turn_failure_model.TurnFailureModel
      version: "2.3.0"  # pinned (turn_failure_model subclasses internals); re-run prebuild.sh after changing
      cost_limit: 0
      config_yaml: |
        agent:
          step_limit: 600
      model_kwargs:
        custom_llm_provider: openai  # chat completions against OPENAI_BASE_URL
        num_retries: 0               # litellm-internal retries off; MSWEA_* above handles them
        timeout: 320                  # per-request cap; a timeout costs one failed turn
        max_tokens: 16384            # per-completion output cap; truncation costs one failed turn
        temperature: 0.7
        top_p: 0.95

datasets:
  - path: tasks
    task_names:
      # - ts-pattern-match-each
      # - happy-dom-abort-pending-body-reads
      - true-myth-iterable-collection-combinators

environment:
  type: docker
  # `mounts` REPLACES Pier's default /logs + /artifacts binds — keep the first three.
  mounts:
    - type: bind
      source: ${HOST_VERIFIER_LOGS_PATH}
      target: ${ENV_VERIFIER_LOGS_PATH}
    - type: bind
      source: ${HOST_AGENT_LOGS_PATH}
      target: ${ENV_AGENT_LOGS_PATH}
    - type: bind
      source: ${HOST_ARTIFACTS_PATH}
      target: ${ENV_ARTIFACTS_PATH}
    # pricing registry (LITELLM_MODEL_REGISTRY_PATH above)
    - type: bind
      source: ${DEEP_SWE_ROOT}/pricing
      target: /opt/deep-swe/pricing
      read_only: true
    # sitecustomize.py + turn_failure_model.py (PYTHONPATH above)
    - type: bind
      source: ${DEEP_SWE_ROOT}/scripts
      target: /opt/deep-swe/scripts
      read_only: true

jobs_dir: jobs
