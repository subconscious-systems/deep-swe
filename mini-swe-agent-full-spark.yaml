# Pier job (full eval, DGX Spark): mini-swe-agent → Baseten Chat Completions (via LiteLLM).
#
# Same as mini-swe-agent-full.yaml except 8 concurrent trials (sized for the
# DGX Spark: 20 cores / 128 GB unified memory; Linux Docker has no VM cap).
# Concurrency runs 8 *different tasks* in parallel — per-task limits are
# unchanged, so results stay leaderboard-comparable:
#   - Agent timeout: task.toml [agent] timeout_sec (5400 s / 90 min)
#   - Steps: mini-swe-agent default (step_limit: 0 = unlimited until timeout)
#   - Attempts: 4 per task (matches official protocol — leaderboard "Pass@1"
#     is the mean per-trial pass rate over 4 trials/task; 452 trials total)
#
# Token pricing (Baseten tim-qwen3.6-27b): pricing/subconscious-tim-qwen3.6-27b.json
#   Input $0.50/1M, cached input $0.05/1M, output $3.50/1M
#
# Set DEEP_SWE_ROOT in .env to this repo's absolute path (bind mount for pricing).
#
# aarch64 note: the prebuilt task images are amd64-only, so this config sets
# force_build: true (native arm64 builds from environment/Dockerfile).
# Pre-build all 113 images once before the eval:
#
#   ./scripts/prebuild.sh    (PREBUILD_JOBS=8 to parallelize)
#   ./run_spark.sh   (wraps: ./scripts/pier-run.sh run -c mini-swe-agent-full-spark.yaml -y)
#
# Resume after interrupt (same YAML; do not change config between start and resume):
#   pier job resume -p jobs/<job-dir>
#
# Laptop full eval: mini-swe-agent-full.yaml • Dev/smoke: mini-swe-agent-dev.yaml

n_concurrent_trials: 8
n_attempts: 4

agents:
  - name: mini-swe-agent
    # Keep in sync with BASETEN_MODEL in .env
    model_name: subconscious/tim-qwen3.6-27b
    env:
      OPENAI_API_KEY: ${BASETEN_API_KEY}
      OPENAI_BASE_URL: ${BASETEN_BASE_URL}
      # Required for custom model slugs Pier/LiteLLM don't recognize (e.g. subconscious/...)
      MSWEA_API_KEY: ${BASETEN_API_KEY}
      LITELLM_MODEL_REGISTRY_PATH: /opt/deep-swe/pricing/model-registry.json
      # Overrides Pier's ignore_errors so unregistered models fail loudly
      MSWEA_COST_TRACKING: default
    kwargs:
      # Use LiteLLM chat completions; avoid the openai/ prefix (that routes to Responses API)
      model_class: litellm
      cost_limit: 0
      model_kwargs:
        # Base URL comes from OPENAI_BASE_URL in agent.env (LiteLLM reads it from the sandbox env)
        custom_llm_provider: openai
        # Per-completion output cap: 16k fits even dense write turns; if a step
        # truncates mid-edit the agent recovers and continues across steps
        max_tokens: 16384
        # Sampling params (passed through to the completion request)
        temperature: 0.7
        top_p: 0.95

datasets:
  - path: tasks

environment:
  type: docker
  # The DGX Spark is aarch64 and the prebuilt task images (task.toml
  # [environment].docker_image) are amd64-only — force native arm64 builds from
  # each task's environment/Dockerfile (mars-base is multi-arch). Warm the
  # layer cache before the eval with ./scripts/prebuild.sh so the per-trial
  # `docker compose build` is a near-instant cache hit.
  force_build: true
  # NOTE: setting `mounts` REPLACES Pier's default /logs + /artifacts bind mounts
  # (pier/environments/docker/docker.py: mounts_json overrides _default_log_mounts).
  # Without them, tee /logs/agent/... exits 1 (NonZeroAgentExitCodeError) and no
  # trajectory/reward files are written — so re-add them here. The HOST_*/ENV_*
  # variables are injected per-trial by Pier when it invokes docker compose.
  mounts:
    - type: bind
      source: ${HOST_VERIFIER_LOGS_PATH}
      target: ${ENV_VERIFIER_LOGS_PATH}
    - type: bind
      source: ${HOST_AGENT_LOGS_PATH}
      target: ${ENV_AGENT_LOGS_PATH}
    - type: bind
      source: ${HOST_ARTIFACTS_PATH}
      target: ${ENV_ARTIFACTS_PATH}
    - type: bind
      source: ${DEEP_SWE_ROOT}/pricing/subconscious-tim-qwen3.6-27b.json
      target: /opt/deep-swe/pricing/model-registry.json
      read_only: true

jobs_dir: jobs
